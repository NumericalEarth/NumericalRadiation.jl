# Documentation Assets

These image assets are generated by the literate examples in
`examples/literate/` when `docs/make.jl` runs. They are committed so the docs
source tree remains self-contained and so links in checked-in generated pages
resolve before a local docs rebuild.

Do not put large validation datasets or temporary run products here. Keep this
directory limited to small images needed by documentation pages.
