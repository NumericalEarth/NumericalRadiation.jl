Manifest.toml
docs/Manifest.toml
docs/build/
docs/site/
docs/src/generated/
*.jl.*.cov
*.jl.cov
*.jl.mem
.DS_Store
.vscode/

# Local developer checkouts of external reference data (ecrad, ecckd source),
# downloaded benchmark inputs and the reports the validation scripts write.
# These are user-provided or artifact-backed via Artifacts.toml; do not
# track them in this repository. The scripts in validation/ are tracked.
validation/external/
validation/data/
validation/results/

# Local session/tooling state
.claude/
.handoff_monitor.log
.handoff_monitor.pid
